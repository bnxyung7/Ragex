import Foundation

/// Preloaded keys - DISABLED
/// All keys must be generated through Admin Panel or API
struct PreloadedKeys {
    /// Initialize preloaded keys on first launch
    static func loadIfNeeded() {
        // No preloaded keys - system disabled
        // All keys must be created via Admin Panel
    }
}

